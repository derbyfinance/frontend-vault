import Layout from '@components/Layout/Layout';
import GamePageComponent from '@components/pageComponents/GamePageComponent';

const index = () => {
  return (
    <Layout>
      <GamePageComponent />
    </Layout>
  );
};

export default index;
